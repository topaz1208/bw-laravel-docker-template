# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
SELECT * FROM todos;
SELECT文。todosテーブルのレコードを全件取得。

### Todoモデルのallメソッドの返り値は何か
Collectionクラスのインスタンス。

### 配列の代わりにCollectionクラスを使用するメリットは
メソッドチェーンによって簡潔な記述が可能となるため可読性が高まり、修正や機能追加も行いやすくなる。

### view関数の第1・第2引数の指定と何をしているか
第１引数　画面に表示するbladeファイルのパスを指定
第２引数　Bladeファイルへ渡すデータを指定
指定したBladeファイルへDBから取得した値を渡し、HTMLを生成・表示させる。

### index.blade.phpの$todos・$todoに代入されているものは何か
$todos：Eloquentのコレクションクラス
$todo：Eloquentモデルのインスタンス（オブジェクト）

## Todo作成機能

### Requestクラスのallメソッドは何をしているか
HTTPリクエストで送信されたすべての入力データを配列として取得する。

### fillメソッドは何をしているか
モデルのプロパティに対して値を一括で代入する。

### $fillableは何のために設定しているか
一括代入による脆弱性を利用した攻撃を防ぐため、更新を許可するカラムを$fillableで指定している。＠

### saveメソッドで実行しているSQLは何か
INSEINSERT INTO todos (content, created_at, updated_at)VALUES(値,値,値);
INSERT文。todoテーブルに新しいレコードを追加し、各カラムに値を挿入する。

### redirect()->route()は何をしているか
指定したルート名に紐づくURLにリダイレクトしている。

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
SQLを直接書かずに、マイグレーション機能でテーブル操作を簡潔に記述出来る。
マイグレーションファイルを共有することで、開発者全員が同一のテーブル構造を再現出来る。

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
up():php artisan migrate
down():php artisan migrate:rollback
マイグレーションの実行と巻き戻し。

### Seederクラスの役割は何か
DBにテスト用の初期データを投入する処理を記述する。

### route関数の引数・返り値・使用するメリット
引数：ルート名
返り値：ルートに紐づくURL
使用するメリット：URLを直接書かずに済むため、Blade内の記述が簡潔になり可読性が向上する。
ルーティング変更の際もルート定義を変えるだけで済み、保守性も向上する。

### @extends・@section・@yieldの関係性とbladeを分割するメリット
@extends：継承する親Bladeの指定
@section：親テンプレートの指定位置に挿入する内容を定義
@yield：子テンプレートの内容を挿入する位置の指定
重複するコードを共通化し再利用を可能にすることで、保守性と開発効率が向上する。

### @csrfは何のための記述か
CSRFトークンを埋め込むための記述。
フォーム送信時にサーバー側でトークンの正当性を検証する。

### {{ }}とは何の省略系か
<?php echo e(); ?>
{{}}内の値に自動的にエスケープ処理が施され画面に出力される。
